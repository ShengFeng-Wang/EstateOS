VITE_API_URL=http://localhost:5266/api

# Google Maps Platform — Map Tiles API key, used for the Photorealistic 3D Tiles
# integration in the Digital Twin. Restrict it (HTTP referrers + API restrictions)
# in Google Cloud Console. Never commit the real value — see .env (gitignored).
VITE_GOOGLE_MAPS_API_KEY=
